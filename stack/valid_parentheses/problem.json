{
  "title": "Valid Parentheses",
  "link": "https://leetcode.com/problems/valid-parentheses/",
  "difficulty": "easy",
  "topic": "stack",
  "solved": true,
  "problem statement": "Given a string s containing just the characters '(', ')', '{', '}', '[' and ']', determine if the input string is valid.\n\nAn input string is valid if:\n\n1. Open brackets must be closed by the same type of brackets.\n2. Open brackets must be closed in the correct order.\n3. Every close bracket has a corresponding open bracket of the same type.",
  "approach": "Use a hash table to map the closed brackets to the open ones. If its an opening bracket we append it to the stack otherwise we check if the the element at the top of the stack matches that bracket and pop it if not then return false. Return true if the stack is empty at the end.",
  "time_complexity": "O(n)",
  "space_complexity": "O(n)"
}