# Product of Array Except Self

**Difficulty:** medium  
**Topic:** Arrays  
**Link:** [LeetCode](https://leetcode.com/problems/product-of-array-except-self/)


## Problem Statement

Given an integer array nums, return an array answer such that answer[i] is equal to the product of all the elements of nums except nums[i]. The product of any prefix or suffix of nums is guaranteed to fit in a 32-bit integer. You must write an algorithm that runs in O(n) time and without using the division operation.


## Approach

The 1st loop provides the product of the numbers to the left of i (prefix) and the second loop goes in the opposite direction and multiplies the prefix with the the product of the numbers to the right of i (postfix).


## Solution

```python
from typing import List


class Solution:
    def productExceptSelf(self, nums: List[int]) -> List[int]:

        prod = [0] * len(nums)
        pre = 1
        post = 1    
        
        for i in range(len(nums)):
            prod[i] = pre
            pre = pre * nums[i]
            
        for i in range(len(nums)-1,-1,-1):
            prod[i] = prod[i] * post
            post = post * nums[i]

        return prod

```


## Test Cases

```python

import json
import os
from solution import Solution

s = Solution()

assert s.productExceptSelf([1,2,3,4]) == [24,12,8,6]
assert s.productExceptSelf([-1,1,0,-3,3]) == [0,0,9,0,0]


print("All tests passed!")
```


## Complexity

**Time:** `O(n)`  
**Space:** `O(1) -  only uses output array `


---
_Auto-generated by GitHub Actions._
